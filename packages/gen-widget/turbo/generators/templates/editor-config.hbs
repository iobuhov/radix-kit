import { {{widgetName}}PreviewProps } from "../typings/{{widgetName}}Props";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: {{widgetName}}PreviewProps, schema: Properties): Properties {
  return schema;
}
