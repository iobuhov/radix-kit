import { ReactElement, createElement } from "react";
import { {{widgetName}}PreviewProps } from "../typings/{{widgetName}}Props";

export function preview({ sampleText }: {{widgetName}}PreviewProps): ReactElement {
    return <div>Preview</div>;
}
