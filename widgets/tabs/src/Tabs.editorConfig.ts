import { TabsPreviewProps } from "../typings/TabsProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TabsPreviewProps, schema: Properties): Properties {
  return schema;
}
