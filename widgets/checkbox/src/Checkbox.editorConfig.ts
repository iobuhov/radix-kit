import { CheckboxPreviewProps } from "../typings/CheckboxProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: CheckboxPreviewProps, schema: Properties): Properties {
  return schema;
}
