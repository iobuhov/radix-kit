import { ButtonPreviewProps } from "../typings/ButtonProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ButtonPreviewProps, schema: Properties): Properties {
  return schema;
}
