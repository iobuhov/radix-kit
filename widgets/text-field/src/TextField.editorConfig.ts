import { TextFieldPreviewProps } from "../typings/TextFieldProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TextFieldPreviewProps, schema: Properties): Properties {
  return schema;
}
