import { AvatarPreviewProps } from "../typings/AvatarProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: AvatarPreviewProps, schema: Properties): Properties {
  return schema;
}
