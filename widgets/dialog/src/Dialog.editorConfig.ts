import { DialogPreviewProps } from "../typings/DialogProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: DialogPreviewProps, schema: Properties): Properties {
  return schema;
}
