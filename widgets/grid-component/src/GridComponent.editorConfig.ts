import { GridComponentPreviewProps } from "../typings/GridComponentProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: GridComponentPreviewProps, schema: Properties): Properties {
  return schema;
}
