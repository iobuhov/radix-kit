import { ProgressPreviewProps } from "../typings/ProgressProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ProgressPreviewProps, schema: Properties): Properties {
  return schema;
}
